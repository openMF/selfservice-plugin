info:
  name: 10. LOGIN PASSWORD CHANGED
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/fineract-provider/api/v1/self/authentication
  headers:
    - name: Fineract-Platform-TenantId
      value: default
  body:
    type: json
    data: |-
      {
          "username": "vilma",
          "password": "MifosInitiative2021#"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
