info:
  name: 06. DEPOSIT TO SAVINGS ACCOUNT
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/fineract-provider/api/v1/savingsaccounts/1/transactions?command=deposit
  headers:
    - name: fineract-platform-tenantid
      value: default
  params:
    - name: command
      value: deposit
      type: query
  body:
    type: json
    data: |-
      {
          "transactionDate": "05 April 2026",
          "transactionAmount": 1000,
          "paymentTypeId": 1,
          "note": "",
          "dateFormat": "dd MMMM yyyy",
          "locale": "en"
      }
  auth:
    type: basic
    username: mifos
    password: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
