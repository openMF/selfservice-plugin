info:
  name: 10. ACTIVATE SAVINGS ACCOUNT 2
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/fineract-provider/api/v1/savingsaccounts/2?command=activate
  headers:
    - name: fineract-platform-tenantid
      value: default
  params:
    - name: command
      value: activate
      type: query
  body:
    type: json
    data: |-
      {
          "activatedOnDate": "05 April 2026",
          "dateFormat": "dd MMMM yyyy",
          "locale": "en"
      }
  auth:
    type: basic
    username: mifos
    password: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
